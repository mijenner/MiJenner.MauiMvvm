<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiMvvm.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiMvvm"
    xmlns:viewmodels="clr-namespace:MauiMvvm.ViewModels"
    x:DataType="viewmodels:MainViewModel">

    <StackLayout Padding="30,0" Orientation="Vertical">
        <Label FontSize="Header" Text=".NET MAUI MVVM architecture" />
        <Label FontSize="Body" Text="{Binding AppName}" />
        <Label FontSize="Body" Text="{Binding CompanyName}" />
        <Label FontSize="Subtitle" Text="Device file path: " />
        <Label FontSize="Body" Text="{Binding FilePath}" />
        <Button
            Command="{Binding GetFilePathCommand}"
            FontSize="Body"
            HorizontalOptions="Fill"
            Text="Read device file path" />
        <Label FontSize="Title" Text="Test IsBusy: " />
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
        <Button
            Command="{Binding TestIsBusyCommand}"
            FontSize="Body"
            HorizontalOptions="Fill"
            Text="Read device file path" />
    </StackLayout>

</ContentPage>
