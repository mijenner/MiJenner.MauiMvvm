<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiMvvm.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiMvvm"
    xmlns:viewmodels="clr-namespace:MauiMvvm.ViewModels"
    Title="{Binding Title}"
    x:DataType="viewmodels:MainViewModel">

    <StackLayout
        Padding="10"
        Orientation="Vertical"
        Spacing="10">
        <Label FontSize="Title" Text=".NET MAUI MVVM architecture" />
        <Label FontSize="Body" Text="{Binding AppName}" />
        <Label FontSize="Body" Text="{Binding CompanyName}" />
        <Label FontSize="Subtitle" Text="Test IsBusy: " />
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
        <Button
            Command="{Binding TestIsBusyCommand}"
            FontSize="Body"
            HorizontalOptions="Fill"
            Text="Test IsBusyCommand" />
        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
            <Button
                Command="{Binding GotoConfigCommand}"
                FontSize="Body"
                HorizontalOptions="Fill"
                Text="Go to Config" />
            <Button
                Grid.Column="1"
                Command="{Binding GotoDetailsCommand}"
                FontSize="Body"
                HorizontalOptions="Fill"
                Text="Go to Details" />


        </Grid>
    </StackLayout>

</ContentPage>
